# v0.7.1 Figma Architecture Diagram Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish a complete editable Figma architecture diagram and matching GitHub README asset for v0.7.1.

**Architecture:** Preserve the existing Figma frames and build a new editable V3 frame in the same file. Export that exact frame to the repository's stable PNG path, update its integrity contract, validate the public projection, and publish the documentation commit without changing the software version.

**Tech Stack:** Figma Plugin API, PNG export, Git, pytest

**Spec:** `docs/superpowers/specs/2026-09-01-v0.7.1-figma-architecture-design.md`

## Global Constraints

- Keep the public product version at `0.7.1`.
- Preserve the existing V1 and V2 Figma frames.
- Use editable Figma frames, text, and vectors for V3. An imported SVG node
  tree is acceptable when the Starter-plan MCP quota blocks further Plugin API
  mutations.
- Export exactly 1920×1080 to the existing public PNG path.
- Do not add runtime dependencies or change product behavior.
- Do not create or move a GitHub release tag.

---

### Task 1: Build and verify the editable V3 Figma frame

**Files:**
- External: Figma file `mIvMHJ1cGoA5mmk9YN3j8L`

**Interfaces:**
- Consumes: approved architecture content from the specification and the existing V2 visual conventions.
- Produces: one top-level `README Architecture V3 — v0.7.1 — 1920×1080` frame and its Figma node ID.

- [ ] **Step 1: Inspect the existing page, V2 frame, fonts, and top-level positions**

  Use read-only Figma metadata and Plugin API inspection. Confirm the V2 frame is `11:2`, the page is `0:1`, and identify available Inter font style names.

- [ ] **Step 2: Clone V2 as a V3 skeleton**

  Clone `11:2`, place the clone to the right of all existing top-level frames, rename it to the required V3 name, mark it as a placeholder, and return every mutated or created node ID.

- [ ] **Step 3: Update existing sections in bounded Figma calls**

  Rewrite the engineer, coding-agent, durable-knowledge, Design MCP, FreeCAD GUI MCP, validation, output, and runtime text using the nodes' existing fonts. Keep every text block inside its frame and return all mutated IDs.

- [ ] **Step 4: Add the design-session and standard-part sections**

  Create native frame and text nodes in the center-lower space for the session workspace and two-stage standard-part discovery. Match the cloned V2 colors and typography.

- [ ] **Step 5: Add labeled control and evidence connectors**

  Create editable vector or line nodes for request, invoke, edit, validate, correction, retrieval, evidence, and lesson-publication flows. Keep arrows behind content cards.

- [ ] **Step 6: Validate the V3 frame**

  Use Figma metadata to verify one 1920×1080 V3 frame, expected named sections, no missing children, and no placeholder state. Use a full-resolution screenshot to verify legibility, no overlaps, no clipping, and complete connectors.

### Task 2: Export and bind the public README asset

**Files:**
- Modify: `docs/assets/ai-mechanical-design-agent-architecture-v2.png`
- Modify: `README.md`
- Modify: `tests/test_public_release_contract.py`

**Interfaces:**
- Consumes: the verified V3 Figma node ID.
- Produces: the exact public PNG and its SHA-256 contract.

- [ ] **Step 1: Export the verified V3 frame**

  Download the Figma node export without overriding its configured natural 1920×1080 size and write it to the stable public PNG path.

- [ ] **Step 2: Verify dimensions and calculate SHA-256**

  Run `sips -g pixelWidth -g pixelHeight docs/assets/ai-mechanical-design-agent-architecture-v2.png` and `shasum -a 256 docs/assets/ai-mechanical-design-agent-architecture-v2.png`. Require exactly 1920×1080.

- [ ] **Step 3: Update the expected public asset digest**

  Replace only the architecture PNG digest in `PUBLIC_README_ASSETS` inside `tests/test_public_release_contract.py`.

### Task 3: Validate and publish the documentation update

**Files:**
- Verify: `README.md`
- Verify: `docs/ARCHITECTURE.md`
- Verify: `public-repository.toml`

**Interfaces:**
- Consumes: the updated PNG and SHA-256 contract.
- Produces: a tested commit on `main` with no version or tag change.

- [ ] **Step 1: Run focused tests**

  Run `uv run --offline --frozen --group test pytest tests/test_public_documentation.py tests/test_public_release_contract.py -q` and require all tests to pass.

- [ ] **Step 2: Run the supported offline suite**

  Run `uv run --offline --frozen --group test pytest -q` and require no unexpected failures.

- [ ] **Step 3: Inspect the release diff**

  Run `git diff --check`, `git status --short`, verify `pyproject.toml` remains `0.7.1`, and verify `v0.7.1` still points to its original commit.

- [ ] **Step 4: Commit, merge, and push**

  Commit the branch as `docs: update architecture diagram for v0.7.1`, merge it into local `main`, rerun the focused verification on `main`, and push `main` to `origin` without creating a tag or release.
